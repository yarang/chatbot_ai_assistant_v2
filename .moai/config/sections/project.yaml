# Project Configuration
# Format: YAML
#
# This file defines project-specific metadata and settings.

name: "Chatbot AI Assistant V2"
type: "web_application"
version: "0.1.0"
description: "LangGraph-based multi-agent AI chatbot with Telegram and web interface"
domains:
  - backend
  - ai
  - database
team_size: solo
development_mode: active
